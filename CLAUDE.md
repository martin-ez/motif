See [AGENTS.md](AGENTS.md).

That file is the single set of instructions for every agent working in this
repository. Keeping the guidance in one place means it cannot drift between
tools; this file exists only so Claude Code finds it.
