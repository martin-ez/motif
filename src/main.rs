//! Entry point for the `motif` binary.
//!
//! A placeholder until loop capture lands. See `docs/brief.md` for scope.

fn main() {
    println!("motif {} — nothing to play yet.", env!("CARGO_PKG_VERSION"));
}
